 <% if blog.favorited_by?(current_user) %>
    <p>
      <%= link_to blog_blog_favorites_path(blog), method: :delete, remote: true do %>
        ♥<%= blog.blog_favorites.count %> good
      <% end %>
    </p>
    <% else %>
    <p>
      <%= link_to blog_blog_favorites_path(blog), method: :post, remote: true do %>
        ♡<%= blog.blog_favorites.count %> good
      <% end %>
    </p>
  <% end %>